FeatureFlag component

Location: app/components/FeatureFlag.jsx

Purpose:
- Toggle UI features on/off using a boolean, key, or predicate function.

Quick test:
1. Open the component file: app/components/FeatureFlag.jsx
2. Render it inside the app or a story with different flag values.
3. Run the project dev server (if applicable) and verify behavior.

Note: This repo also contains a SwitchCaseComponent at app/components/SwitchCaseComponent.tsx as a reference for conditional rendering patterns.
